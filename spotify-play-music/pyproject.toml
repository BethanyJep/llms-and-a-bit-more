[project]
name = "spotify_play_music"
version = "0.1.0"
description = "MCP server for controlling Spotify playback on macOS using AppleScript"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "mcp==1.23.0"
]

[project.optional-dependencies]
dev = ["debugpy==1.8.8"]